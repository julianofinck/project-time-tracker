repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4  # Replace with the latest version of Ruff
    hooks:
      - id: ruff
        args: ["--fix", "--select", "I"]  # 🧹 only isort-like rules
      - id: ruff
        args: ["--fix"]  # 🧼 fix everything else
